package com.sosks1313.command;

import java.util.ArrayList;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.sosks1313.dao.bDao;
import com.sosks1313.dto.BDto;

public class BListCommand implements BCommand{
/*
	@Override
	public void execute(HttpServletRequest request, HttpServletResponse response) {

		bDao dao = new bDao(); // bDao 생성
		ArrayList<BDto> dtos = dao.lists(); //bDao에 있는 list()메소드 사용 후 배열 dtos에 넣는다
		
		request.setAttribute("list", dtos); //리퀘스트의 list의 속성값을 dtos로 저장
	*/
	}
}
